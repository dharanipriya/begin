#include<stdio.h>
#include<conio.h>
void main()
{
int n,a[i],sum=0;
float avg;
clrscr();
printf("\n enter the num of element in an array:");
for(i=0;i<n;i++)
scanf("\n%d",&a[i]);
for(i=o;i<10;i++)
{
sum=sum+a[i];
avg=sum/10;
printf("\n average of an array elements:",avg);
}
getch();
}
