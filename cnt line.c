#include<stdio.h>
#include<conio.h>
void main()
{
int cnt=1,len,n;
char s[100];
printf("\n string:");
for(i=0;i<n;i++)
scanf("\n %s",s[i]);
len=strlen(s[i]);
for(i=0;i<len;i++0
{
if(a[i]=='\n')
cnt=cnt+1;
printf("\n %d",cnt);
}
getch();
}
