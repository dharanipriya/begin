#include<stdio.h>
#include<conio.h>
void main()
{
char s[100];
int i,count=0;
clrscr();
printf("\n enter the string":);
scanf("\n %s",s[i]);
for(i=0;s[i]!='\0';i++)
{
if(s[i]==' ')
count++;
printf("\n %d number of words in an array",count+1);
}
getch();
}
